export enum UIState {
  NONE = "none",
  SELECTED = "selected",
  HOLD = "hold",
  DISBANDED = "disbanded",
  DISLODGED = "dislodged",
  BUILD = "build",
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum ScrollButtonState {
  BACK = "left",
  FORWARD = "right",
  ENABLED = "enabled",
}
