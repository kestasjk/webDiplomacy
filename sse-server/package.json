{
  "name": "sse-server",
  "version": "1.0.0",
  "description": "Events server to replace websockets/pusher with simple self-hosted SSE server to notify clients of new messages and game updates",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "redis": "^5.1.1"
  }
}
